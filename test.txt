DATA Arvind
DATA D[4]
CONST E=0
DATA B
DATA C
START:
MOV A, AX
MOV BX, C
ADD AX, BX, CX
SUB EX, BX, DX
MUL GX, EX, HX
X:
ADD BX, AX, DX
IF CX EQ AX THEN
MOV BX, C
IF BX LTEQ DX THEN
ADD AX, BX, CX
SUB EX, BX, DX
ELSE
MOV A, AX
MUL GX, EX, HX
ENDIF
PRINT Arvind
ENDIF
JMP X
END